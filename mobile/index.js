import { registerRootComponent } from 'expo';

import App from './App';

// registerRootComponent ensures that the environment is set up appropriately
// whether you load the app in Expo Go or in a native build.
registerRootComponent(App);
